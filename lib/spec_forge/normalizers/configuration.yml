base_url:
  type: string
  required: true

global_variables:
  type: hash
  default: {}

factories:
  type: hash
  default: {}
  structure:
    ###########################################
    auto_discover:
      type: boolean
      default: true

    paths:
      type: array
      default: []
      structure:
        type: [string, pathname]
