create_user:
  path: /users
  method: POST
  expectations:
  - variables:
      name: Test User
      email: faker.internet.email
    body:
      name: variables.name
      email: variables.email
    expect:
      status: 200
      json:
        id: kind_of.integer
        name: variables.name
        email: test@example.com
