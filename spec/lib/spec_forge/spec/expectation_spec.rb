# frozen_string_literal: true

RSpec.describe SpecForge::Spec::Expectation do
  describe "#compile" do
    context "when input is an empty hash"
    context "when input is a valid hash"
    context "when input is not a hash"
    context "when variables is not a hash"
  end
end
